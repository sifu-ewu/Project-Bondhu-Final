# ===================================================
# SECURITY WARNING: NEVER COMMIT .env FILE TO GIT
# ===================================================

# Django Settings
# Generate a new secret key using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-unique-secret-key-here-never-use-default

# Environment Configuration
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database Configuration (PostgreSQL)
# Create a new database and user with strong password
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_strong_database_password
DB_HOST=localhost
DB_PORT=5432

# Email Configuration (Optional - for notifications)
# Use app-specific passwords for Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-app-specific-password
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ===================================================
# DEVELOPMENT ONLY - Example for local development
# ===================================================
# SECRET_KEY=dev-only-key-for-local-testing
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1
# DB_NAME=bondhu_dev
# DB_USER=postgres
# DB_PASSWORD=dev_password_123
